---
title: Bí Danh Archives | Dotfiles
description: Tên lệnh nén rút gọn. Tổng quan cho Dotfiles v0.2.495. Tương thích với macOS, Linux và WSL qua Dotfiles.
lang: vi-VN
author: Sebastien Rousseau
lastUpdated: true
image: /logo.png
imageAlt: Bí Danh Archives | Dotfiles preview
canonical: /vi/aliases/archives/
robots: index, follow
draft: false
noindex: false
sitemap: true
breadcrumbTitle: Bí danh Archives
pageType: docs
schemaType: WebPage
permalink: /vi/aliases/archives/
---

# Bí Danh Archives

Tên lệnh nén rút gọn.

Trang này giới thiệu một nhóm bí danh và các phím tắt lệnh thường dùng.
Đọc trang này trước để hiểu quy ước đặt tên, đầu vào mong đợi và mẫu thực thi an toàn.
Sau đó mở phụ lục tham khảo để xem toàn bộ danh sách lệnh và cú pháp chính xác.
Các bí danh này giảm việc gõ lặp lại và duy trì hành vi quy trình làm việc nhất quán trên macOS, Linux và WSL.

## Khi Nào Sử Dụng

Sử dụng trang này trước công việc shell hàng ngày khi bạn cần mẫu lệnh dự đoán được.

## Bao Gồm

- Bí danh: 40
- Hàm hỗ trợ: 3
- Tệp nguồn: 1

## Ví Dụ Nhanh

- `ac` → `'compress'        # Nén chung (Archive Create)`
- `acl` → `'compress_large' # Nén lớn kiểu cũ`
- `bak` → `'backup'         # Hàm sao lưu nhanh`
- `c7z` → `'7z a' # Tạo kho 7z`
- `cbz` → `'bzip2 -zk' # Nén bằng bzip2`
- `cgz` → `'gzip -cv' # Nén bằng gzip`
- `clz4` → `'lz4 -zc' # Nén bằng lz4`
- `crar` → `'rar a' # Tạo kho rar`
- `ctar` → `'tar -cvf'         # Tạo kho tar`
- `ctbz` → `'tar -jcvf'        # Tạo kho tar.bz2`
- `ctgz` → `'tar -zcvf'        # Tạo kho tar.gz`
- `ctxz` → `'tar -Jcvf'        # Tạo kho tar.xz`

## Bước Tiếp Theo

Mở danh sách đầy đủ:

