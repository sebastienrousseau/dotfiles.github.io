---
title: Bí Danh Cd | Dotfiles
description: Lệnh di chuyển thư mục rút gọn. Tổng quan cho Dotfiles v0.2.495.
lang: vi-VN
author: Sebastien Rousseau
lastUpdated: true
image: /logo.png
imageAlt: Bí Danh Cd | Dotfiles preview
canonical: /vi/aliases/cd/
robots: index, follow
draft: false
noindex: false
sitemap: true
breadcrumbTitle: Bí danh Cd
pageType: docs
schemaType: WebPage
permalink: /vi/aliases/cd/
---

# Bí Danh Cd

Lệnh di chuyển thư mục rút gọn.

Trang này giới thiệu một nhóm bí danh và các phím tắt lệnh thường dùng.
Đọc trang này trước để hiểu quy ước đặt tên, đầu vào mong đợi và mẫu thực thi an toàn.
Sau đó mở phụ lục tham khảo để xem toàn bộ danh sách lệnh và cú pháp chính xác.
Các bí danh này giảm việc gõ lặp lại và duy trì hành vi quy trình làm việc nhất quán trên macOS, Linux và WSL.

## Khi Nào Sử Dụng

Sử dụng trang này trước công việc shell hàng ngày khi bạn cần mẫu lệnh dự đoán được.

## Bao Gồm

- Bí danh: 16
- Hàm hỗ trợ: 6
- Tệp nguồn: 12

## Ví Dụ Nhanh

- `bm` → `'bm add'            # Tạo đánh dấu`
- `bmg` → `'cd "$(bm goto $1)"' # Đi đến đánh dấu`
- `bml` → `'bm list'           # Liệt kê đánh dấu`
- `bmr` → `'bm remove'         # Xóa đánh dấu`
- `bmu` → `'bm update'         # Cập nhật đánh dấu`
- `cd` → `'cd_with_history' # Ghi đè lệnh cd mặc định`
- `cdh` → `'cd_with_history' # Hàm cd nâng cao`
- `cdhelp` → `cd_aliases_help`
- `cdversion` → `cd_aliases_version`
- `dh` → `'dirhistory' # Hiện lịch sử thư mục`
- `dirs` → `'dirs -v'        # Liệt kê ngăn xếp thư mục`
- `ld` → `'lwd'        # Quay lại thư mục cuối`

## Bước Tiếp Theo

Mở danh sách đầy đủ:

