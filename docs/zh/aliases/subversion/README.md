---
description: Subversion 别名用于与 `svn` 交互。Subversion 是版本控制系统。
lang: zh-CN
metaTitle: Subversion 别名 - Dotfiles (ZH)
permalink: /zh/aliases/subversion/

meta:
  - name: keywords
    content: aliases, subversion, svn, linux, macos, shell, terminal, windows
  - name: twitter:card
    content: Subversion 别名用于与 `svn` 交互。Subversion 是版本控制系统。
  - name: twitter:description
    content: Subversion 别名用于与 `svn` 交互。Subversion 是版本控制系统。
  - name: twitter:title
    content: Subversion 别名 - Dotfiles (ZH)
  - name: og:title
    content: Subversion 别名 - Dotfiles (ZH)
  - name: og:description
    content: Subversion 别名用于与 `svn` 交互。Subversion 是版本控制系统。
  - name: og:image:alt
    content: Dotfiles - 为你的 Shell 生活而设计
  - name: og:locale
    content: zh_CN
---
# Subversion 别名

管理 Subversion 别名。**Universal Dotfiles** 配置的一部分。

![Dotfiles banner][banner]

## 📖 描述

这些别名定义在 `subversion.aliases.sh` 中，并由 `chezmoi` 自动加载。

## ⚡ 别名

Subversion CLI 别名：
- `sad` 将新文件/目录纳入版本控制。
- `sau` 管理缓存的认证凭据。
- `sbl` 显示每行最后更改时间。
- `scg` 关联/取消 changelist。
- `sci` 将更改提交到仓库。
- `scl` 恢复中断操作或移除不需要文件。
- `sco` 从仓库检出工作副本。
- `scp` 复制文件/目录。
- `sct` 输出指定文件/URL 内容。
- `sdi` 显示本地更改或差异。
- `sdl` 从版本控制移除文件/目录。
- `shp` 显示子命令用法。
- `sin` 显示本地/远程条目信息。
- `sip` 提交未版本控制文件/目录。
- `slg` 查看日志。
- `slk` 锁定路径/URL。
- `sls` 列出仓库目录条目。
- `smd` 创建版本控制目录。
- `smg` 合并更改。
- `smgi` 显示合并信息。
- `smv` 移动/重命名。
- `sp` 设置属性。
- `spdl` 删除属性。
- `spdt` 用外部编辑器编辑属性。
- `spgt` 输出属性值。
- `sph` 应用补丁。
- `spls` 列出属性。
- `srl` 重新定位工作副本。
- `srs` 解决冲突。
- `srsd` 移除冲突状态。
- `srv` 恢复 pristine 状态。
- `sst` 输出状态。
- `ssw` 更新到同仓库不同 URL。
- `sulk` 解锁路径/URL。
- `sup` 从仓库更新。
- `supg` 升级元数据格式。
- `sxp` 创建未版本控制的树拷贝。

[banner]: https://kura.pro/dotfiles/v2/images/titles/title-dotfiles.svg
