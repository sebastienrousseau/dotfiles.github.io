---
description: NPM 别名用于与 `npm` 命令行工具交互。Npm 是 JavaScript 的包管理器。
lang: zh-CN
metaTitle: NPM 别名 - Dotfiles (ZH)
permalink: /zh/aliases/npm/

meta:
  - name: keywords
    content: aliases, npm, node, node package manager, package manager, linux, macos, shell, terminal, windows
  - name: twitter:card
    content: NPM 别名用于与 `npm` 命令行工具交互。Npm 是 JavaScript 的包管理器。
  - name: twitter:description
    content: NPM 别名用于与 `npm` 命令行工具交互。Npm 是 JavaScript 的包管理器。
  - name: twitter:title
    content: NPM 别名 - Dotfiles (ZH)
  - name: og:title
    content: NPM 别名 - Dotfiles (ZH)
  - name: og:description
    content: NPM 别名用于与 `npm` 命令行工具交互。Npm 是 JavaScript 的包管理器。
  - name: og:image:alt
    content: Dotfiles - 为你的 Shell 生活而设计
  - name: og:locale
    content: zh_CN
---
# Npm 别名

管理 Npm 别名。**Universal Dotfiles** 配置的一部分。

![Dotfiles banner][banner]

## 📖 描述

这些别名定义在 `npm.aliases.sh` 中，并由 `chezmoi` 自动加载。

## ⚡ 别名

此代码提供 [npm](https://www.npmjs.com/) 的别名集合。
- `npa` 审计 npm 包。
- `npb` npm build 脚本。
- `npc` npm 缓存。
- `npd` npm dev 脚本。
- `npg` 全局 npm 包。
- `npi` 安装 npm 包。
- `npl` 列出 npm 包。
- `npp` 发布 npm 包。
- `nprm` 删除 npm 包。
- `npr` 运行 npm 脚本。
- `nprw` 运行 npm 脚本 watch。
- `nps` 启动 npm 脚本。
- `npsv` npm serve 脚本。
- `npt` 运行测试脚本。
- `npu` 更新 npm 包。
- `npx` 执行 npm 包。
- `npy` 查询 npm 包为何安装。

[banner]: https://kura.pro/dotfiles/v2/images/titles/title-dotfiles.svg
