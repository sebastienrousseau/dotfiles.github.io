This project is a static site generated with VuePress.

## Prerequisites

- Node.js
- pnpm

## Development

```bash
pnpm install
pnpm run dev
```

Open `http://localhost:8080` to view the site.

## Deployment

This repository is deployed to GitHub Pages via the `deploy.sh` script or GitHub Actions.
